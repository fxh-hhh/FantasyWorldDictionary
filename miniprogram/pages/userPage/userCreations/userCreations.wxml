<!--pages/userPage/userCreations/userCreations.wxml-->
<block wx:for="{{worksinfo}}" wx:key="_id">
    <view>
        <navigator url="../../mainPage/wordClause/wordClause?id={{item._id}}">{{item.call}}</navigator>
    </view>
</block>
